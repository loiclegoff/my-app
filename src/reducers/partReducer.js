
const initialState = {
  parts: [],
  /*...*/
}

/**
 * Reducer
 */
const partReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default partReducer